import React from 'react';


const StartupOptions = () => {
    return (
    
        <div className = "wrapper">
            <div className="startup" id="dashboard">Dashboard</div>
            <div className="startup">Add Startup</div>
            <div className="startup">Startup Details</div>
        </div>
    );
}

export default StartupOptions;
